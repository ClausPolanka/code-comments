package sageniuz;

public class WordCounter {

    /**
     * Input text's lines are split by white space(s). Derived word candidates
     * will only be counted if word candidates consist of latin alphabet
     * characters only.
     * <p/>
     * Examples:
     * <pre>
     *            "inputText" / word count
     *                "hello" / 1
     *          "hello world" / 2
     *  "  hello    world   " / 2
     *               "hello," / 0
     *               "hello." / 0
     *               "hel1lo" / 0
     *              "$hel1lo" / 0
     *         "hello, world" / 1
     *         "hello\nworld" / 2
     * </pre>
     *
     * @param inputText text to count words from
     * @return word count
     */
    public int countWords(String inputText) {
        return 0;
    }
}
